# Temp Readme

This is a snippet of most of the applicable code, submitted as part of the interim demo. The biggest changes are in the es_env and callback Python files. 

SPACE has been implemented, and is currently being used to change the instance curricula, but it seems that there may be some issues with the value estimation calculations. The program currently always trains on instances in the same order (set at the start of that run, then staying consistent but not necessarily staying the same across different runs), which seems unlikely to be correct. Further testing and analysis is required, but the code is nearly complete. It mostly requires a potential fix to that value estimation, and to be better documented and formatted. 

